<!DOCTYPE html>
<html>
    <head>
        <title>Услуги 3d печати на принтере <%= prntr.name %> </title>
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body style="margin-top:50px">
        <%- include nav.ejs %>
        <div class="container">
            <h1 class="text-center"><%= prntr.name %></h1>
            <hr>
            <div class="row">
                    <div class="col-xs-12" id="slider">
                        <!-- Top part of the slider -->
                        <div class="row">
                            <div class="col-sm-6" id="carousel-bounding-box">
                                <div class="carousel slide" id="myCarousel">
                                    <!-- Carousel items -->
                                    <div class="carousel-inner">
                                        <% for(var i = 0, lgth = prntr.pics.length; i < lgth; i++) {%>
                                            <div class="<% if (i === 0) {%> active <%}%> item" data-slide-number="<%= i %>">
                                            <img src="<%= prntr.pics[i] %>" style="width:100%"></div>
                                        <% } %>
                                    </div><!-- Carousel nav -->
                                    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                                        <span class="glyphicon glyphicon-chevron-left"></span>                                       
                                    </a>
                                    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                                        <span class="glyphicon glyphicon-chevron-right"></span>                                       
                                    </a>                                
                                    </div>
                            </div>

                            <div class="col-sm-6" id="carousel-text"></div>

                            <div id="slide-content" class="text-center">
                                <div class="row hidden-xs" id="slider-thumbs">
                                    <!-- Bottom switcher of slider -->
                                    <ul class="hide-bullets" style="list-style: none">
                                        <% for(var i = 0, lgth = prntr.thumbs.length; i < lgth; i++) {%>
                                            <li class="col-sm-1">
                                                <a class="thumbnail" id="carousel-selector-<%= i %>"><img src="<%= prntr.thumbs[i] %>"></a>
                                            </li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                </div><!--/Slider-->
                
                            <div class="row">
                    <h2 class="text-center">Материалы, доступные для печати на принтере <%= prntr.name %></h2>
                    <% for(var j = 0, lgth = mtrls.length; j < lgth; j++) {%>
                    <div class="col-xs-12" id="slider">
                        <!-- Top part of the slider -->
                        <div class="row">
                            <div class="col-sm-6" id="carousel-bounding-box">
                                <div class="carousel slide" id="myCarousel">
                                    <!-- Carousel items -->
                                    <div class="carousel-inner">
                                        <% for(var i = 0, lgth = mtrls[j].pics.length; i < lgth; i++) {%>
                                            <div class="<% if (i === 0) {%> active <%}%> item" data-slide-number="<%= i %>">
                                            <img src="<%= mtrls[j].pics[i] %>" style="width:100%"></div>
                                        <% } %>
                                    </div><!-- Carousel nav -->
                                    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                                        <span class="glyphicon glyphicon-chevron-left"></span>                                       
                                    </a>
                                    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                                        <span class="glyphicon glyphicon-chevron-right"></span>                                       
                                    </a>                                
                                    </div>
                            </div>

                            <div class="col-sm-6" id="carousel-text"></div>

                            <div id="slide-content" class="text-center">
                                <div class="row hidden-xs" id="slider-thumbs">
                                    <!-- Bottom switcher of slider -->
                                    <ul class="hide-bullets" style="list-style: none">
                                        <% for(var i = 0, lgth = mtrls[j].thumbs.length; i < lgth; i++) {%>
                                            <li class="col-sm-1">
                                                <a class="thumbnail" id="carousel-selector-<%= i %>"><img src="<%= mtrls[j].thumbs[i] %>"></a>
                                            </li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                    <% } %>
                </div><!--/Slider-->
                
                <hr>
                
                <h2 class="text-center">Заказать печать</h2>
                
        </div>
        <script src="/js/jquery-1.10.2.min.js"></script>
        <script src="/js/flat-ui.min.js"></script>
        <script src="/js/3dmodel.js"></script>
    </body>
</html>