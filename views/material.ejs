<!DOCTYPE html>
<html>
    <head>
        <title>Услуги 3d печати из материала <%= mtrl.name %> </title>
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body style="margin-top:50px">
        <%- include nav.ejs %>
        <div class="container">
            <h1 class="text-center"><%= mtrl.name %></h1>
            <hr>
                
            <div class="row">
                    <h2 class="text-center">Образцы печати</h2>
                    <div class="col-xs-12" id="slider">
                        <!-- Top part of the slider -->
                        <div class="row">
                            <div class="col-sm-6" id="carousel-bounding-box">
                                <div class="carousel slide" id="myCarousel">
                                    <!-- Carousel items -->
                                    <div class="carousel-inner">
                                        <% for(var i = 0, lgth = mtrl.pics.length; i < lgth; i++) {%>
                                            <div class="<% if (i === 0) {%> active <%}%> item" data-slide-number="<%= i %>">
                                            <img src="<%= mtrl.pics[i] %>" style="width:100%"></div>
                                        <% } %>
                                    </div><!-- Carousel nav -->
                                    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                                        <span class="glyphicon glyphicon-chevron-left"></span>                                       
                                    </a>
                                    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                                        <span class="glyphicon glyphicon-chevron-right"></span>                                       
                                    </a>                                
                                    </div>
                            </div>

                            <div class="col-sm-6" id="carousel-text">
                                <div class="row">
                                    <h2 class="text-center">Характеристики материала</h2>
                                    <table class="table">
                                        <tr>
                                            <td>
                                                Относительное удлинение на разрыв
                                            </td>
                                            <td>
                                                зн
                                            </td>
                                        </tr>
                                    </table>
                                </div>
                            </div>

                            <div id="slide-content" class="text-center">
                                <div class="row hidden-xs" id="slider-thumbs">
                                    <!-- Bottom switcher of slider -->
                                    <ul class="hide-bullets" style="list-style: none">
                                        <% for(var i = 0, lgth = mtrl.thumbs.length; i < lgth; i++) {%>
                                            <li class="col-sm-1">
                                                <a class="thumbnail" id="carousel-selector-<%= i %>"><img src="<%= mtrl.thumbs[i] %>"></a>
                                            </li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                </div><!--/Slider-->
                
                            <div class="row">
                    <h2 class="text-center">Оборудование, на котором производится печать</h2>
                    <% for(var j = 0, lgth = prntrs.length; j < lgth; j++) {%>
                    <div class="col-xs-12" id="slider">
                        <!-- Top part of the slider -->
                        <div class="row">
                            <div class="col-sm-6" id="carousel-bounding-box">
                                <div class="carousel slide" id="myCarousel">
                                    <!-- Carousel items -->
                                    <div class="carousel-inner">
                                        <% for(var i = 0, lgth = prntrs[j].pics.length; i < lgth; i++) {%>
                                            <div class="<% if (i === 0) {%> active <%}%> item" data-slide-number="<%= i %>">
                                            <img src="<%= prntrs[j].pics[i] %>" style="width:100%"></div>
                                        <% } %>
                                    </div><!-- Carousel nav -->
                                    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                                        <span class="glyphicon glyphicon-chevron-left"></span>                                       
                                    </a>
                                    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                                        <span class="glyphicon glyphicon-chevron-right"></span>                                       
                                    </a>                                
                                    </div>
                            </div>

                            <div class="col-sm-6" id="carousel-text"></div>

                            <div id="slide-content" class="text-center">
                                <div class="row hidden-xs" id="slider-thumbs">
                                    <!-- Bottom switcher of slider -->
                                    <ul class="hide-bullets" style="list-style: none">
                                        <% for(var i = 0, lgth = prntrs[j].thumbs.length; i < lgth; i++) {%>
                                            <li class="col-sm-1">
                                                <a class="thumbnail" id="carousel-selector-<%= i %>"><img src="<%= prntrs[j].thumbs[i] %>"></a>
                                            </li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                    <% } %>
                </div><!--/Slider-->
                
                <hr>
                
                <h2 class="text-center">Заказать печать</h2>
                
        </div>
        <script src="/js/jquery-1.10.2.min.js"></script>
        <script src="/js/flat-ui.min.js"></script>
        <script src="/js/3dmodel.js"></script>
    </body>
</html>