<!DOCTYPE html>
<html>
    <head>
        <title>Услуги 3d печати</title>
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
        <link href="/css/calc.css" rel="stylesheet">
    </head>
    <body style="margin-top:50px">
        
        <%- include nav.ejs %>
        <div class="container">
            <h1 class="text-center">3d принтеры, доступные для заказа 3d печати</h1>
            <hr>
            <% for(var i = 0, lgth = prntrs.length; i < lgth; i++) {%>
                <% if ((i%4) === 0) { %> <div class="row"> <% } %>
                <div class="col-md-3">
                    <div class="thumbnail">
                        <a href="/3dprinters/<%= prntrs[i].name %>">
                            <img src="<%= prntrs[i].pics[0] %>" class="img-responsive">
                            <div class="caption">
                                <h3 class="text-center"><%= prntrs[i].name %></h3>
                            </div>
                        </a>
                    </div>
                </div>
                <% if ((i%4) === 3) { %> </div> <% } %>
            <% } %>
                
        </div>
        <script src="/js/jquery-1.10.2.min.js"></script>
        <script src="/js/flat-ui.min.js"></script>
    </body>
</html>